import React from 'react'
import { StyledHeader } from './styles';
import { Container } from "../Container";

export const Header = () => (
  <StyledHeader>
    <Container>
      Header title
    </Container>
  </StyledHeader>
);
