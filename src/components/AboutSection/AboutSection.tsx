import React from 'react'
import { Section } from '../Section';

interface Props {
  
}

export const AboutSection = (props: Props) => {
  return (
    <Section>
      <h2>Section</h2>
    </Section>
  )
};
